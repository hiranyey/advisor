# Used only by `vite dev` / `vite preview` — points at the standalone FastAPI dev
# server (`uv run fastapi dev main.py`, defaults to :8000, routes mounted under /api).
# Production builds (Docker image) fall back to the same-origin /api default in api.js.
VITE_API_BASE=http://localhost:8000/api
